interface:
  display_name: "Explorable"
  short_description: "Turn long markdown into a single-file HTML explorable"
  default_prompt: "Use $explorable to turn this markdown into a single-file HTML explorable explanation."
policy:
  allow_implicit_invocation: false
